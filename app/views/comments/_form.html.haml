= form_for([@post, @comment],:html => {:class => "form-inline mt-3 mb-3"}, remote: true) do |f|
  - if logged_in?
    .form-group.comment-input-wrapper
      = f.text_field :content, placeholder: "コメントする", maxlength: "86", class: "form-control comment-input mr-2"
    .form-group.comment-btn-wrapper 
      = f.submit '送信', class: "btn btn-success btn-circle comment-btn"
  - else
    .form-group.comment-input-wrapper
      = f.text_field :content, placeholder: "コメント機能を利用するにはログインしてください", class: "form-control comment-input mr-2", disabled: true
    .form-group.comment-btn-wrapper 
      = f.submit '送信', class: "btn btn-success btn-circle comment-btn", disabled: true