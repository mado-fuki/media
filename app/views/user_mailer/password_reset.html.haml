%p
  = @user.name + " 様"

%p
  パスワードリセットのリクエストが行われました。
%p
  この手続きを完了するには、下記のリンクをクリックしてください。

= link_to "パスワードをリセット", edit_password_reset_url(@user.reset_token,
                                                      email: @user.email)
%p
  このリンクの有効期限は2時間です。